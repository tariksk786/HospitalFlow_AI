// ============================================
// HospitalFlow AI — Login Entry
// MedFlow Vista Visual Standard
// ============================================

import { renderAuthLanding } from './auth-portal.js';

export default function renderLoginPage(container) {
  renderAuthLanding(container);
}
